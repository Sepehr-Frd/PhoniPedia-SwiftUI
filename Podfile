# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PhoniPedia (SwiftUI)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PhoniPedia (SwiftUI)
	
	pod 'Alamofire'

  target 'PhoniPedia (SwiftUI)Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PhoniPedia (SwiftUI)UITests' do
    # Pods for testing
  end

end
